[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []

[project]
name = "employee-tracker"
version = "0.1.0"
description = "A simple employee tracker application"
requires-python = ">=3.9"
dependencies = [
    "flask>=3.1.3",
]
